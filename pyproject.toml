[project]
name = "strava-mcp"
version = "0.1.0"
description = "A Python-native Strava MCP server for deep activity analysis."
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.1.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.1",
    "pandas>=2.2.0"
]

[project.optional-dependencies]
dev = ["ruff>=0.3.0"]
